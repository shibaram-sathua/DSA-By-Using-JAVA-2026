public class StringBuilderClass {
    public static void main(String[] args){
        //in string buffer all methods are synchronised it implies that no two methods can execute simultaneously at the same time they have to finish one's work.
        StringBuilder s = new StringBuilder();
        //this works asynchronously

    }

}
