public class Practice1 {
    // string are immutable
    //having the reference of available called name. and pointing to string object.
    //the string object is created on string pool constant pool
    //string are immutable
    //it will not create not other objectin string constant pool if both are having same values
    //String a = "Rahul";
    //String b = "Rahul";
    //both a and b objects are pointing to the same object
    //here we did not change the object we just created a new object and a points to the new object
}
